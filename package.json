{
  "name": "smart-city-sim",
  "version": "1.0.0",
  "description": "Smart-City Traffic Simulation API (GraphQL + TypeScript)",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env --import tsx ./src/index.ts",
    "dev": "tsx watch ./src/index.ts"
  },
  "dependencies": {
    "@apollo/server": "^4.10.0",
    "graphql": "^16.9.0"
  },
  "devDependencies": {
    "tsx": "^4.16.2",
    "typescript": "^5.6.3"
  }
}